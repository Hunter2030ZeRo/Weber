[package]
name = "weber-cli"
version = "0.1.0-alpha.1"
edition = "2021"
license = "Apache-2.0"

[[bin]]
name = "weber"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
